{
  "name": "experimenting-with-ai",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run . --watch",
    "build": "bun build . --outdir=dist",
    "start": "bun run dist/index.js",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
